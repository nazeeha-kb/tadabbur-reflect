export const metadata = {
  title: "Dashboard | Tadabbur",
  description: "Your reflection journey, streaks, and profile at a glance.",
};

export default function DashboardLayout({ children }) {
  return children;
}
